import random
'''
class Walker:
    def __init__(self, position):
        """
        - self.x, self.y: current position
        """
    
    def step(self):
        """
        Brownian motion on square lattice:
        - Choose random direction: up, down, left, right
        - Move one step in that direction
        - Return new position
        """
        # Algorithm: 
        # directions = [(0,1), (0,-1), (1,0), (-1,0)]
        # dx, dy = random.choice(directions)
        # self.x += dx
        # self.y += dy
    
    def distance_from(self, position):
        """Euclidean distance from given position"""
'''